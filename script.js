// dom elements 
    // output screen display 
    const outputScreen = document.querySelector("#output-screen");
    // equals / submit button
    const equalsButton = document.querySelector("#equals-button")
    // clear button
    const clearButton = document.querySelector("#clear-button");
    // decimal place button
    const decimalPlaceButton = document.querySelector("#decimalPlace-Button");
    // logical operator buttons
    const plusMinusButton = document.querySelector("#plusMinus-button")
    const percentButton = document.querySelector("#percent-button");
    const divideButton = document.querySelector("#divide-button");
    const multiplyButton = document.querySelector("#multiply-button");
    const minusButton = document.querySelector("#minus-button");
    const plusButton = document.querySelector("#plus-button");
    // numeric buttons 
    const zeroButton = document.querySelector("#zero-button");
    const oneButton = document.querySelector("#one-button");
    const twoButton = document.querySelector("#two-button");
    const threeButton = document.querySelector("#three-button");
    const fourButton = document.querySelector("#four-button");
    const fiveButton = document.querySelector("#five-button");
    const sixButton = document.querySelector("#six-button");
    const sevenButton = document.querySelector("#seven-button");
    const eightButton = document.querySelector("#eight-button");
    const nineButton = document.querySelector("#nine-button");





// convert the string values to int
const getTheButtonInputAsNumber = (event) => {
    const UserInput = event.target.value;
    // use switch
};




// global variables




// functions


// event listeners
    // equals/submit button
    equalsButton.addEventListener("click", listener)
    // clear button
    clearButton.addEventListener("click", dosomething);
    // decimal place button
    decimalPlaceButton.addEventListener("click", listener)
    // logical operator buttons
    plusMinusButton.addEventListener("click", listener)
    percentButton.addEventListener("click", listener)
    divideButton.addEventListener("click", listener)
    multiplyButton.addEventListener("click", listener)
    minusButton.addEventListener("click", listener)
    plusButton.addEventListener("click", listener)
    // numeric buttons
    zeroButton.addEventListener("click", listener)
    oneButton.addEventListener("click", listener)
    twoButton.addEventListener("click", listener)
    threeButton.addEventListener("click", listener)
    fourButton.addEventListener("click", listener)
    fiveButton.addEventListener("click", listener)
    sixButton.addEventListener("click", listener)
    sevenButton.addEventListener("click", listener)
    eightButton.addEventListener("click", listener)
    nineButton.addEventListener("click", listener)


